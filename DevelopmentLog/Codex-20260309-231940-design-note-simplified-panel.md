# 设计弹层右侧内容简化

时间：2026-03-09 23:19:40
执行者：Codex

## 本次改动

- 更新了 `src/components/DesignNoteModal.vue` 的右侧信息区结构。
- 删除了左侧图片轮播下方的缩略图条。
- 删除了右侧顶部的头像、名称、时间和关注按钮。
- 删除了右侧底部的“阅读全文”按钮和底部统计区。
- 右侧现在只保留标题、摘要和正文内容，结构更干净。
- 同时清理了对应的未使用样式和不再需要的传参。

## 关联调整

- `src/views/DesignView.vue` 不再给弹层传 `tagName`，也不再监听弹层里的文章详情跳转。
- `src/App.vue` 移除了设计页不再需要的 `tagName` 透传。

## 验证结果

- `npm run lint` 通过
- `npm run build` 通过
