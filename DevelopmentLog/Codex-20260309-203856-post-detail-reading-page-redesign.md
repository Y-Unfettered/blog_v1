# 文章详情页阅读体验重构

时间：2026-03-09 20:38:56
执行者：Codex

## 本次改动

- 重做了 `src/views/PostDetailView.vue` 的页面结构，把原来的单层正文页改成了完整的阅读页布局。
- 新增了头部信息区，包含返回按钮、分类标签、置顶标识、文章摘要、发布时间、阅读时长和最后更新时间。
- 新增了封面图区、标签区、移动端目录区和更完整的上一篇/下一篇卡片导航。
- 桌面端目录改成右侧粘性目录卡，不再是原来那种左侧悬浮小面板。
- 为正文补齐了完整的排版样式，包括标题层级、段落、列表、引用、表格、图片、分隔线、代码块和复制按钮样式。
- `src/App.vue` 中给详情页补传了 `tagMap`，并把代码复制成功提示改成中文。
- `src/utils/blog.js` 中的代码块复制按钮默认文案从 `Copy` 改成了 `复制代码`。

## 验证结果

- `npm run lint` 通过
- `npm run build` 通过

## 影响文件

- `src/views/PostDetailView.vue`
- `src/App.vue`
- `src/utils/blog.js`
