# 设计创作页瀑布流重构

时间：2026-03-09 22:31:35
执行者：Codex

## 本次改动

- 重写了 `src/views/DesignView.vue`，把原来的普通三列卡片网格改成了瀑布流布局。
- 使用 CSS columns 实现多列瀑布流，桌面端 4 列，平板 3/2 列，手机 1 列。
- 新增了横向滚动的分类筛选条，并做成粘性顶部过滤栏。
- 设计卡片改成图片主导的内容卡，信息结构更接近灵感流/小红书式浏览体验。
- 为卡片加入了不同的封面高度模式，让页面不再是整齐划一的等高网格。
- 重写了页头文案、统计信息、空状态和卡片底部信息展示。

## 验证结果

- `npm run lint` 通过
- `npm run build` 通过
